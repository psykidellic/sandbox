import ChannelAssignment._

import scala.collection.mutable.HashMap

object HelloWorld {
  def main(args: Array[String]) {

    //val r = assignChannel(null)
    //println(r)
    //XXX.ritesh()

    val x = new HashMap[Int, Int]()
    x + (1 -> 1)

    println(x.toString())

  }
}
